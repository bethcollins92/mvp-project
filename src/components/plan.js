import React, { Component } from "react";

export default class plan extends Component {
  render() {
    return (
      <div>
        when save button is clicked in choose time - come to this page to show
        final plan and times for notifications.
      </div>
    );
  }
}
